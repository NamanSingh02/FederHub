# Demo Client Data

These are synthetic sample datasets and checkpoints for Phase 2 demos.

- `fracture-detection/`: toy fracture-risk training bundle
- `tumor-detection/`: toy tumor-detection training bundle
- `sample_client_data.csv` and `sample_model.pt`: generic baseline demo bundle

Each subfolder is designed to work as a selectable local dataset folder inside the Electron app.
